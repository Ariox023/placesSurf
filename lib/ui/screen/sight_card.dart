import 'package:flutter/material.dart';

class SightCard extends StatelessWidget {
  const SightCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
